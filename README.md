# WIP docker compose to set up OpenLdap with phpldapadmin

# Server 500 Error
APP_KEY must be set manually or with a script because the Environment Variables are not set automatically
# pw.sha2 exit
older versions can cause issues and newer ones might aswell this error does not occur with bitnami2.6 and phpldapadmin2
# admin creds not working
create admin user with ldif
# automate any further conf commands

# Goal
execute docker-compose and have a fresh openldap which always works
